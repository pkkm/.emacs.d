;;; Move the mouse when the cursor comes near it.

(mouse-avoidance-mode 'animate)
;; 'jump -- displace the mouse by a random distance.
;; 'animate -- the same, but animate.

(provide 'conf/view/gui/mouse-avoidance)
