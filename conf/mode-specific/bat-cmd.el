;;; DOS batch and Windows CMD scripts.
;; This file doesn't do anything. It's just a reminder and place for future customization.

(use-package dos ; Installed in `my-vendor-dir'.
  :defer t)

(provide 'conf/mode-specific/bat-cmd)
